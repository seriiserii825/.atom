"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const index_1 = require("./index");
const assert = require("assert");
it('smoke test', () => {
    assert(index_1.inject);
});
it('provide number', () => {
    let result = index_1.inject('number', () => 1);
    assert(result === 1);
});
it('provide object', () => {
    let dep = { foo: 'be' };
    let result = index_1.inject('dep', () => dep);
    assert(result === dep);
});
it('mock test', () => {
    index_1.injector.mock('id', () => 'foo');
    let result = index_1.inject('id', () => 'buz');
    assert(result === 'foo');
});
it('clear', () => {
    index_1.injector.mock('foo', () => 'FOO');
    index_1.injector.clear();
    let result = index_1.inject('foo', () => '123');
    assert(result === '123');
});
//# sourceMappingURL=index.spec.js.map
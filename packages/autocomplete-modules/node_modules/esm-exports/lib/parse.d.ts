import * as ts from 'typescript';
import { Entry } from './entry';
export declare type ParseOptions = {
    filepath?: string;
    module?: string;
    statements?: ts.Statement[];
};
export declare function parse(sourceText: string, options?: ParseOptions): Promise<Entry[]>;

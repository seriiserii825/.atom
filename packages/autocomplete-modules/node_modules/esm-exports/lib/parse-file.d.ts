import { Entry } from './entry';
export declare type ParseFileOptions = {
    dirname?: string;
    module?: string;
};
export declare function parseFile(file: string, options: ParseFileOptions): Promise<Entry[]>;

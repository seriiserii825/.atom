import { Entry } from './entry';
export declare type ParseModuleOptions = {
    dirname?: string;
    module?: string;
};
export declare function parseModule(name: string, options?: ParseModuleOptions): Promise<Entry[]>;
